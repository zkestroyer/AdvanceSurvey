import 'dart:convert'; void main() { String json = '{"sections":[{"title":"Test","questions":[{"questionText":"Technology","type":"text"}]}]}'; var parsed = jsonDecode(json); var sections = parsed['sections']; for (var s in sections) { for (var q in s['questions']) { if (q['questionText'].toString().toLowerCase() == 'technology') { q['type'] = 'dropdown'; q['options'] = jsonEncode(['Poly', 'Mono']); } } } print(jsonEncode(parsed)); }